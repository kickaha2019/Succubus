<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render "head" relative_url: resource.relative_url %}
  </head>
  <body class="{{ resource.data.layout }} {{ resource.data.page_class }}">
    {% render "navbar", metadata: site.metadata, resource: resource %}

    <main>
        <div class="medium_width">
          {{ resource.relations.articles.name }}
          {% for article in collections.articles.resources %}
            <article>
              <a href="{{ article.relative_url }}">{{ article.data.title }}</a>

              <p>{{ article.data.description }}</p>
            </article>
          {% endfor %}
        </div>
        
        <div class="medium_width">
          <h2>Recent posts</h2>
          
          {% for post in paginator.resources %}
            <article>
              <a href="{{ post.relative_url }}">{{ post.data.title }}</a>

              <p>{{ post.data.description }}</p>
            </article>
          {% endfor %}
          
          <a class="black" "href="section-{{ section }}-posts">All posts</a>
        </div>
    </main>

    {% render "footer" %}
  </body>
</html>
