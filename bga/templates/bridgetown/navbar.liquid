<nav>
<div>British <span>Go</span> Association<div>
</nav>

{% if resource.parents %}
  <div class="breadcrumbs">
    {% for parent in resource.parents %}
      <a href="{{ parent.url }}">{{ parent.name }}</a> &raquo;
    {% endfor %}
    {{ resource.name }}
  </div>
{% endif %}
